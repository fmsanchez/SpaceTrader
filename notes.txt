This commit:
	> added getPosition() to StarSystem
    > added rotate() to Position
    > added origin to Position
    > added StarType to StarSystem
    > moved src/json to src/main/java
	> Made galaxy generation logic

system generator needs to have access to galaxy
planet generator needs to have access to system

systems need a way to be passed the generator type
planets need a way to be passed the generator type

system generator type needs a way to be passed the galaxy
galaxy needs to be shared among the system generator types

